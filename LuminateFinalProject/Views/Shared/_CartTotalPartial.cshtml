@model IEnumerable<BasketVM>
<div class="sub-content">
    <h5>SubTotal</h5>
    <span class="subtotal">$@Model.Sum(c => c.Count * c.Price)</span>
</div>
<hr>
<div class="total-content">
    <h4>Total</h4>
    <span class="subtotal">$@(@Model.Sum(c => c.Count * c.Price))</span>
</div>